{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "atlas-main",
  "description": "Permite que a janela principal do Atlas Code use IPC, eventos Tauri e comandos nativos necessários para PTY, bridge e cartografia.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
