# Cartografia State

Responsavel por graph snapshot, note cache, recent changes, erros, loading e
maquina de navegacao `universe -> system -> flow -> gear -> subflow`.

Nao pode conter JSX, CSS, DOM measurement ou acoes externas.
