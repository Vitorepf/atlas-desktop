{
  "name": "@atlas/domain",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Atlas Code · TypeScript domain types shared between the React cockpit and any future surface (mobile, CLI). Mirrors the Atlas Server contracts.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
